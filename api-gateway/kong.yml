_format_version: "3.0"
services:
  - name: transfer-service
    url: http://transfer-service:3000
    routes:
      - name: transfer-route
        paths: ["/transfer"]
        methods: ["POST"]
        plugins:
          - name: jwt